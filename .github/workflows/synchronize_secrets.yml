name: synchronize secrets
on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: Log level
        required: true
        default: warning
      tags:
        description: synchronize secrets
jobs:
  job1:
    name: synchronize secrets
    runs-on: ubuntu-latest
    steps:
    - name: 更新nichuanfang/config-server的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/movie-tvshow-spider的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/movie-tvshow-spider
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/wechat-girlfriend-push的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/wechat-girlfriend-push
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/music-tool-kit的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/music-tool-kit
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/bitwarden-backup的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/bitwarden-backup
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog-vercel的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog-vercel
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥GH_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: GH_TOKEN
        value: ${{ secrets.GH_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/movie-tvshow-spider的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/movie-tvshow-spider
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/blog-docs-next
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥BARK_KEY
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: BARK_KEY
        value: ${{ secrets.BARK_KEY }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/config-server的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/config-server
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/xray-parser的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/xray-parser
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/docker的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/docker
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nichuanfang.github.io的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nichuanfang.github.io
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/crawler的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/crawler
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nginx-config的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nginx-config
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/MyScripts的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/MyScripts
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/nogfw的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/nogfw
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/blog-docs-next 的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: 'nichuanfang/blog-docs-next '
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/tgbot的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/tgbot
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_HOST
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_HOST
        value: ${{ secrets.VPS_HOST }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_DOMAIN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_DOMAIN
        value: ${{ secrets.VPS_DOMAIN }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_USER
        value: ${{ secrets.VPS_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_PASS
        value: ${{ secrets.VPS_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_PORT
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_PORT
        value: ${{ secrets.VPS_PORT }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_CONFIG_USER
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_CONFIG_USER
        value: ${{ secrets.VPS_CONFIG_USER }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/api的密钥VPS_CONFIG_PASS
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/api
        name: VPS_CONFIG_PASS
        value: ${{ secrets.VPS_CONFIG_PASS }}
        pa_token: ${{ secrets.GH_TOKEN }}
    - name: 更新nichuanfang/hexo-blog-vercel的密钥VERCEL_TOKEN
      uses: jon-grey/github-actions-secrets-creator@v1
      with:
        location: nichuanfang/hexo-blog-vercel
        name: VERCEL_TOKEN
        value: ${{ secrets.VERCEL_TOKEN }}
        pa_token: ${{ secrets.GH_TOKEN }}
